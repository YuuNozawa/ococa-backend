<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace ="dev.ococa.api.infrastructure.repository.setting.SettingMapper">
    <select id="getSettingByUserId" resultType="dev.ococa.api.domain.model.setting.SettingEntity">
        SELECT * FROM settings WHERE user_id = #{id}
    </select>
</mapper>